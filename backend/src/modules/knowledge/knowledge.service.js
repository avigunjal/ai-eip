// Knowledge concentration business logic.
// TODO: compute coverage, single-owner systems, transfer opportunities.

export async function getSystemById(id) {
  return { id };
}

export async function listAreas() {
  return [];
}
