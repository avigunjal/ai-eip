// SQL migrations live here, one file per migration.
// 001_initialSchema.sql contains the full portable schema (people, teams,
// projects, capabilities, knowledge, risks, scenarios, recognition). The
// migration runner applies files in filename order and records them in
// schema_migrations. Delete data/ai-eip.db to reset from scratch.